/* STM32 port shim: PN7462 EEPROM configuration is not present; the RC663 port
   uses fixed compile-time settings, so no EEPROM accessors are needed. */
#ifndef PHCFG_EE_SHIM_H
#define PHCFG_EE_SHIM_H
#endif
